# 사출금형 견적 시뮬레이터 개발 스택 정의서 V1.0

> **개발 스택 정의서** — 무엇으로, 어떤 구조로, 어떤 순서로 만들 것인지 확정하는 문서. 6단계(개발)의 착수 기준이다.

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-09-18 |
| 버전 | V1.0 |
| 선행 문서 | `5.금형견적_UI디자인시안.md` |
| 산출물 | `index.html` (단일 파일) |

---

## 1. 기술 스택

| 구분 | 기술 | 버전 | 용도 | 조달 |
|---|---|---|---|---|
| Frontend 마크업 | HTML5 | — | 단일 문서 구조 | 직접 작성 |
| Frontend 스타일 | Tailwind CSS (Play CDN) | 3.x | 유틸리티 기반 레이아웃·컴포넌트 | `https://cdn.tailwindcss.com` |
| Frontend 폰트 | Pretendard | 1.3.x | 한글 본문 서체 | CDN, 실패 시 시스템 폰트 폴백 |
| 로직 | Vanilla JavaScript (ES2015+) | — | 계산 엔진, 상태 관리, DOM 바인딩 | 직접 작성 |
| 저장소 | Web Storage API (`localStorage`) | 표준 | 마스터 데이터, 견적 이력 | 브라우저 내장 |
| 출력 | `window.print()` + CSS `@media print` | 표준 | A4 견적서 생성 | 브라우저 내장 |
| Backend | **없음** | — | — | — |
| Database | **없음** (localStorage로 대체) | — | — | — |
| API | **없음** | — | — | — |
| Deployment | 파일 배포 (공유 폴더 / USB / 메일 첨부) | — | — | — |

### 1.1 도입하지 않는 것과 그 이유

| 미도입 기술 | 이유 |
|---|---|
| React / Vue 등 프레임워크 | 화면 1개, 상태 객체 2개. 프레임워크 런타임이 계산 로직보다 무겁다 |
| 빌드 도구 (Vite, webpack) | REQ-16(더블클릭 실행)과 정면 충돌. 빌드 산출물을 배포하려면 빌드 환경을 유지해야 한다 |
| npm / package.json | 의존성이 CDN 2개뿐이라 매니저가 관리할 대상이 없다 |
| **jsPDF + jspdf-autotable** | **D-01 결정으로 제외.** 한글 렌더링 불가 → 폰트 임베드 시 +2~3MB. 브라우저 인쇄가 의존성 0으로 같은 결과를 낸다 |
| 테스트 프레임워크 (Jest 등) | 순수 함수 몇 개를 검증하는 데 러너·설정 파일이 코드보다 커진다. `__selfTest()` 함수 하나로 대체 |
| 아이콘 라이브러리 | 아이콘 10개를 위해 폰트 파일을 추가하지 않는다. 유니코드 이모지 사용 |
| 상태 관리 라이브러리 | 전역 객체 `master`, `input`, `result` 3개가 전부 |

> 💡 **판단 기준** — "이 의존성이 없으면 못 만드는가?"에 아니오라면 넣지 않는다. 단일 파일 배포라는 제약에서는 의존성 하나가 곧 배포 실패 지점 하나다.

### 1.2 CDN 차단 환경 대응 (오프라인 판)

사내망이 외부 CDN을 차단하면 앱이 스타일 없이 뜨거나 아예 동작하지 않는다. 대응 순서:

1. **1차:** Tailwind Play CDN으로 개발·검증한다.
2. **2차:** 최종 화면에서 실제 사용된 CSS만 추출해 `<style>` 블록에 인라인한다.
3. **3차:** Pretendard를 시스템 폰트(맑은 고딕)로 대체한다.
4. 결과물 `index_offline.html`은 외부 요청 0건으로 동작한다.

> 인라인 판은 파일이 커지지만(약 30~50KB) 네트워크 의존이 사라진다. **납품용 최종본은 오프라인 판을 기본으로 한다.**

---

## 2. 프로젝트 구조

```
견적서/
├── Research.md                          # 원천 조사 자료
├── plan.md                              # 개발기획 요약
├── 1.금형견적_요구사항명세서_SRS.md      # 1단계 산출물
├── 2.금형견적_기능명세서_PRD.md          # 2단계 산출물
├── 3.금형견적_정보구조도_IA.md           # 3단계 산출물
├── 4.금형견적_와이어프레임.md            # 4단계 산출물
├── 5.금형견적_UI디자인시안.md            # 5단계 산출물
├── 6.금형견적_개발스택정의서.md          # 6단계 산출물 (본 문서)
├── 7.금형견적_테스트결과보고서.md        # 7단계 산출물
├── index.html                           # ★ 실행 파일 (CDN 판)
└── index_offline.html                   # ★ 납품본 (인라인 판, Phase 5)
```

### 2.1 `index.html` 내부 구조

```
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">                    ← 한글 깨짐 방지, 최상단 필수
  <meta name="viewport" ...>
  <title>사출금형 견적 시뮬레이터</title>
  <script src="tailwind CDN"></script>
  <style> /* @media print 규칙, 커스텀 토큰 */ </style>
</head>
<body>
  <div id="app" class="no-print">          ← 화면 UI
    <header>...</header>                    ← 제목 + 버튼 3개
    <main>
      <section id="inputPane">...</section> ← SCR-01 좌측 (섹션 A~D)
      <aside  id="resultPane">...</aside>   ← SCR-01 우측 결과
    </main>
    <footer>...</footer>
  </div>

  <div id="masterModal" hidden>...</div>    ← SCR-02
  <div id="historyModal" hidden>...</div>   ← SCR-03
  <div id="toastArea"></div>                ← SCR-05
  <div id="printArea">...</div>             ← SCR-04 (화면 숨김, 인쇄 전용)

  <script>
    /* ── 1. 상수/기본 마스터 ───────── */  DEFAULT_MASTER
    /* ── 2. 상태 ─────────────────── */  let master, input, result
    /* ── 3. 저장소 계층 ───────────── */  loadMaster/saveMaster/loadHistory/saveHistory
    /* ── 4. 순수 계산 함수 ─────────── */  calcMaterial/calcMachining/calcMechanism/calcFinal
    /* ── 5. 검증 ─────────────────── */  clamp/validate
    /* ── 6. 렌더링 ───────────────── */  renderLabels/renderResult/renderWarnings
    /* ── 7. 이벤트 바인딩 ──────────── */  이벤트 위임 1개소
    /* ── 8. 모달/토스트 ───────────── */  openModal/closeModal/toast
    /* ── 9. 출력 ─────────────────── */  buildPrintDoc/printQuote
    /* ── 10. 자가검증 ─────────────── */  __selfTest()
    /* ── 11. 초기화 ───────────────── */  init()
  </script>
</body>
</html>
```

**계층 규칙:**

| 규칙 | 내용 |
|---|---|
| L-1 | **4번(계산 함수)은 순수 함수다.** DOM에 접근하지 않고 입력 객체와 마스터만 받아 결과 객체를 반환한다 |
| L-2 | DOM 읽기·쓰기는 6·7번 계층에서만 한다 |
| L-3 | 저장소 접근은 3번 계층을 통해서만 한다. `localStorage`를 여기저기서 직접 부르지 않는다 |
| L-4 | 화면의 단가·임율 라벨은 6번 `renderLabels()`가 마스터에서 생성한다. HTML에 숫자를 쓰지 않는다 (plan.md R7) |

> **L-1이 핵심이다.** 계산 함수가 순수하면 `__selfTest()`가 화면 없이 검증할 수 있고, 계산 오류와 표시 오류를 분리해 추적할 수 있다. 원문 코드는 계산과 DOM 조작이 한 함수에 섞여 있어 이 분리가 없었다.

---

## 3. 보안 및 데이터 취급

| 항목 | 이행 사항 |
|---|---|
| API 키 | **존재하지 않는다.** 외부 서비스를 호출하지 않으므로 관리할 자격 증명이 없다 |
| 외부 전송 | CDN 리소스 요청 외 네트워크 호출 0건 (REQ-13). `fetch`·`XMLHttpRequest`·폼 `action` 사용 금지 |
| 영업 기밀 | 강재 단가·공정 임율·마진율은 회사의 원가 구조다. 브라우저 밖으로 나가지 않는다 |
| 고객 정보 | 고객사명·프로젝트명은 `localStorage`에만 저장. 수집·전송·분석 없음 |
| 분석 도구 | Google Analytics 등 추적 스크립트 삽입 금지 |
| 개발 중 데이터 | 테스트에는 실제 고객사명 대신 더미 값(`A정밀(주)`, `B테크`) 사용 |
| 문서 공유 | 본 문서군에 실제 회사 단가를 기입하지 않는다. 기본값은 조사 자료 기준 예시값이다 |
| 코드 리뷰 | AI가 생성한 계산식은 **반드시 손계산과 대조**한 뒤 확정한다 (7번 문서) |
| 파일 배포 | 저장된 마스터는 파일이 아닌 브라우저에 있다. 파일만 전달하면 단가는 전달되지 않는다 → FN-10 JSON 내보내기 병행 |

> ⚠️ **오해 주의** — "localStorage에 저장했으니 안전하다"가 아니다. 같은 PC를 쓰는 다른 사람이 같은 브라우저를 열면 단가가 보인다. 이 앱의 보안 모델은 **"PC에 대한 물리적 접근 통제"**에 의존한다. 공용 PC에 두지 않는다.

---

## 4. 개발 순서 (점진적 개발)

### Phase 1 — 동작하는 베이스라인 ★ 필수
| # | 작업 | 완료 기준 |
|---|---|---|
| 1-1 | HTML 골격 + Tailwind CDN + `@media print` 스텁 | 빈 화면이 스타일과 함께 뜬다 |
| 1-2 | `DEFAULT_MASTER` 정의 + 저장소 계층 | 콘솔에서 `master` 객체 확인 |
| 1-3 | 순수 계산 함수 4종 (FN-01~04) | 콘솔 호출로 T1 손계산 일치 |
| 1-4 | 입력 검증 (FN-06) | 음수·문자 입력 시 0 보정 + 경고 반환 |
| 1-5 | SCR-01 입력 폼 + 결과 패널 마크업 | 와이어프레임 배치 재현 |
| 1-6 | 이벤트 위임 + 렌더링 (FN-05) | 입력 변경 시 즉시 갱신 |
| 1-7 | `renderLabels()` 로 단가·임율 표기 생성 (R7) | 마스터 수정 시 라벨도 함께 변경 |
| 1-8 | `__selfTest()` 구현 (FN-11) | 콘솔에서 T1~T3 통과 |
| **검증** | **콘솔 오류 0건 / T1 손계산 오차 0원** | |

> **Phase 1에서 반드시 고칠 원문 결함:** 템플릿 리터럴 백틱 누락(R2·R3 — 이것 때문에 원문은 아무 값도 표시하지 못한다), CDN 경로 오류(R1), 닫는 태그 누락(R4).

### Phase 2 — 견적 정확도
| # | 작업 | 완료 기준 |
|---|---|---|
| 2-1 | 복잡도 드롭다운 + 가공비 가중 (FN-02, R6) | 일반→초정밀 시 가공비만 1.5배 |
| 2-2 | 캐비티 계수 반영 (D-02) | 캐비티 1→2 시 가공비 1.15배 |
| 2-3 | 일반관리비 항목 (D-03) | 마진 이후 가산 확인 |
| 2-4 | 견적 정보 입력부 (고객사·프로젝트·견적일·유효기간, D-04) | 입력값이 출력물에 전달 |
| **검증** | **T4(초정밀) 손계산 일치, 재료비·기구비 불변** | |

### Phase 3 — 출력
| # | 작업 | 완료 기준 |
|---|---|---|
| 3-1 | `#printArea` 견적서 DOM 구성 (SCR-04) | 인쇄 미리보기에 A4 1페이지 |
| 3-2 | `@media print` 규칙 완성 | 화면 UI 전부 숨김 |
| 3-3 | 문서번호 생성 `Q-YYYYMMDD-NN` | 같은 날 2건이면 -01, -02 |
| 3-4 | 고지 문구 고정 (REQ-19) | 어떤 조건에서도 표시 |
| 3-5 | 출력 전 검증 (고객사명·경고 확인) | E8-1·E8-2 동작 |
| **검증** | **출력물 7개 금액이 화면과 100% 일치, 한글 정상** | |

### Phase 4 — 데이터 보존
| # | 작업 | 완료 기준 |
|---|---|---|
| 4-1 | SCR-02 마스터 편집 모달 (탭 4개) | 전 항목 수정 가능 |
| 4-2 | 저장·초기화 + 토스트 | 재시작 후 값 유지 |
| 4-3 | SCR-03 이력 저장·목록·불러오기·삭제 | 50건 FIFO 동작 |
| 4-4 | JSON 내보내기·가져오기 (FN-10) | 다른 PC에서 동일 단가 적용 |
| **검증** | **재시작 후 마스터·이력 유지, 불러오기 시 입력 전부 복원** | |

### Phase 5 — 마감
| # | 작업 | 완료 기준 |
|---|---|---|
| 5-1 | 1366×768 노트북 레이아웃 확인 | 가로 스크롤 없음 |
| 5-2 | 접근성 점검 (라벨·포커스·`aria-live`) | 키보드만으로 전 기능 사용 |
| 5-3 | 오프라인 판 `index_offline.html` 생성 | 네트워크 차단 상태에서 정상 동작 |
| 5-4 | 7번 테스트 결과 보고서 작성 | 전 케이스 실행 및 기록 |
| **검증** | **테스트 Pass율 100%, 미해결 Critical 0건** | |

**Phase 1~3이 실사용 최소 단위**다. Phase 4~5 없이도 견적 작성과 출력은 가능하다.

---

## 5. 기능 단위 개발 지시 공식

각 작업은 다음 5요소를 갖춰 지시한다. (현재 상태 → 수정 목표 → 변경 범위 → 금지사항 → 완료 기준)

**예시 — 작업 2-1:**

| 요소 | 내용 |
|---|---|
| 현재 상태 | `calcMachining()`이 공정시간 × 임율만 합산한다 |
| 수정 목표 | 형상 복잡도 배수를 가공비에 적용한다 |
| 변경 범위 | `calcMachining()` 함수, 섹션 D의 `select#complexity`, `DEFAULT_MASTER.complexity` |
| 금지사항 | 재료비·기구비 계산에 손대지 말 것. 다른 섹션의 마크업을 재배치하지 말 것. 배수를 함수에 하드코딩하지 말 것(마스터 참조) |
| 완료 기준 | T1 입력에서 복잡도를 초정밀로 변경 시 가공비가 정확히 1.5배, 재료비·기구비·중량은 불변. `__selfTest()` 통과 |

---

## 6. 버전 관리

현재 프로젝트는 Git 저장소가 아니다. 두 가지 선택지:

| 방안 | 장점 | 단점 | 권장 |
|---|---|---|---|
| A. `git init` 후 Phase별 커밋 | 변경 추적, 되돌리기, 차이 확인 | Git 학습 필요 | ★ 권장 |
| B. 파일명 버전 관리 (`index_v1.html`) | 즉시 가능 | 변경 이유 추적 불가, 파일 난립 | 차선 |

**A 선택 시 커밋 단위:** Phase 내 작업 번호별로 1커밋 (예: `feat(2-1): 형상 복잡도 가중치 적용`).

---

## 7. 완료 체크리스트

- [x] 기술 스택이 확정되었는가 → 1장 (미도입 결정과 근거 포함)
- [x] 프로젝트 구조가 정의되었는가 → 2장 (파일 구조 + 파일 내부 11계층)
- [x] 보안 이행 사항이 정리되었는가 → 3장
- [x] 개발 순서가 Phase·작업 단위로 분할되었는가 → 4장 (5 Phase, 24개 작업)
- [x] 지시 공식이 마련되었는가 → 5장
- [ ] **기능별 단위 개발 및 커밋이 완료되었는가 → 코드 구현 착수 전 (미완)**
