# 사출금형 견적 시뮬레이터 UI 디자인 시안 V1.0

> **UI 디자인 시안** — 색·타이포·컴포넌트 상태를 확정하는 문서. 개발자가 임의로 색을 고르지 않도록 토큰 단위로 못 박는다.

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-09-18 |
| 버전 | V1.0 |
| 선행 문서 | `4.금형견적_와이어프레임.md` |
| 구현 방식 | Tailwind CSS 유틸리티 클래스 (CDN) |

---

## 1. 디자인 컨셉

### 1.1 톤앤매너
**"공장 사무실의 계기판"** — 장식을 배제하고 숫자의 가독성만을 좇는다.

| 원칙 | 적용 |
|---|---|
| 숫자가 주인공 | 금액은 고정폭(monospace) 서체로 자릿수가 세로로 정렬되게 한다 |
| 입력과 결과의 명암 대비 | 입력부는 밝은 배경, 결과부는 어두운 배경. 시선이 결과로 끌린다 |
| 장식 최소화 | 그라데이션·일러스트·애니메이션 없음. 상태 변화는 색과 테두리로만 |
| 경고는 눈에 띄게 | 잘못된 견적이 조용히 넘어가지 않도록 색+아이콘+텍스트 3중 표기 |

### 1.2 컬러 팔레트

| 역할 | 토큰 | HEX | Tailwind | 용도 |
|---|---|---|---|---|
| Primary (결과 패널 배경) | `--c-panel` | `#0F172A` | slate-900 | 우측 결과 패널, 인쇄 표 헤더 |
| Primary Deep | `--c-panel-deep` | `#020617` | slate-950 | 최종가 강조 영역 |
| Secondary (구획 배경) | `--c-section` | `#F8FAFC` | slate-50 | 좌측 입력 섹션 카드 |
| Accent (강조/최종가) | `--c-accent` | `#10B981` | emerald-500 | 최종 견적가 숫자, 주 버튼 |
| Accent Sub (소계) | `--c-accent-sub` | `#FCD34D` | amber-300 | 제조원가 소계 숫자 |
| Info (보조 라벨) | `--c-info` | `#2DD4BF` | teal-400 | 결과 패널 제목 |
| Background | `--c-bg` | `#F1F5F9` | slate-100 | 페이지 바탕 |
| Surface | `--c-surface` | `#FFFFFF` | white | 카드·모달 바탕 |
| Border | `--c-border` | `#E2E8F0` | slate-200 | 구분선, 입력 테두리 |
| Text Primary | `--c-text` | `#1E293B` | slate-800 | 본문 |
| Text Muted | `--c-text-muted` | `#64748B` | slate-500 | 라벨, 단위, 보조 설명 |
| Text Inverse | `--c-text-inv` | `#F1F5F9` | slate-100 | 어두운 패널 위 텍스트 |
| Success | `--c-success` | `#059669` | emerald-600 | 저장 성공 토스트 |
| Warning | `--c-warning` | `#D97706` | amber-600 | 입력 범위 경고 |
| Error | `--c-error` | `#DC2626` | red-600 | 음수 최종가, 저장 실패, 오류 테두리 |

**대비비 검증 (WCAG AA 기준 4.5:1)**

| 조합 | 대비비 | 판정 |
|---|---|---|
| slate-800 on white | 12.6:1 | ✅ AAA |
| slate-500 on slate-50 | 4.8:1 | ✅ AA |
| emerald-500 on slate-900 | 7.1:1 | ✅ AAA |
| amber-300 on slate-900 | 10.4:1 | ✅ AAA |
| red-600 on white | 5.9:1 | ✅ AA |
| teal-400 on slate-900 | 8.3:1 | ✅ AAA |

> ⚠️ **경고 표시에 색만 쓰지 않는다.** 적록색약 사용자를 위해 모든 경고에 아이콘(⚠)과 문장을 함께 넣는다. 색은 보조 신호다.

### 1.3 다크 모드
**지원하지 않는다.** 사용 환경이 주간 사무실 PC로 고정되어 있고, 결과 패널이 이미 어두운 대비 구조라 다크 모드에서 명암 위계가 무너진다. 개발·검증 비용 대비 효용이 없다.

---

## 2. 타이포그래피

| 용도 | 폰트 | 크기 | 굵기 | 행간 | 색 |
|---|---|---|---|---|---|
| 앱 제목 (H1) | Pretendard, 시스템 sans | 24px | 700 Bold | 1.3 | slate-900 |
| 섹션 제목 (H2) | 동일 | 18px | 600 SemiBold | 1.4 | slate-700 |
| 결과 패널 제목 | 동일 | 20px | 700 Bold | 1.4 | teal-400 |
| 입력 라벨 | 동일 | 12px | 500 Medium | 1.4 | slate-500 |
| 입력 값 | 동일 | 14px | 400 Regular | 1.5 | slate-800 |
| 본문·설명 | 동일 | 14px | 400 Regular | 1.6 | slate-600 |
| 보조 설명(단가 표기) | 동일 | 11px | 400 Regular | 1.4 | slate-400 |
| **결과 금액** | **고정폭 monospace** | 14px | 500 Medium | 1.5 | slate-100 |
| **최종 견적가** | **고정폭 monospace** | 30px | 900 Black | 1.2 | emerald-500 |
| 경고 문구 | Pretendard | 13px | 500 Medium | 1.5 | amber-600 / red-600 |

**폰트 지정:**
```css
font-family: Pretendard, -apple-system, "Segoe UI", "Malgun Gothic", sans-serif;
/* 금액 전용 */
font-family: "SF Mono", Consolas, "D2Coding", monospace;
font-variant-numeric: tabular-nums;  /* 자릿수 폭 고정 — 숫자가 바뀌어도 흔들리지 않음 */
```

> 💡 **`tabular-nums`가 중요한 이유** — 실시간 재계산으로 금액이 계속 바뀌는 화면에서 비례폭 숫자를 쓰면 값이 바뀔 때마다 글자가 좌우로 떨린다. 고정폭 숫자는 자릿수가 제자리에 머물러 시선이 안정된다.

**웹폰트 정책:** Pretendard를 CDN으로 불러오되, 차단된 환경을 대비해 시스템 폰트(맑은 고딕)로 폴백한다. 폰트 하나 때문에 앱이 안 뜨는 일은 없어야 한다.

---

## 3. 컴포넌트 스타일 가이드

### 3.1 버튼

| 종류 | 용도 | Default | Hover | Active | Disabled |
|---|---|---|---|---|---|
| Primary | 견적서 출력 | bg emerald-500 / text slate-900 / 700 / py-3 / rounded-lg / shadow-lg | emerald-600 | emerald-700 | slate-400, cursor-not-allowed |
| Secondary | 견적 저장, 단가설정, 이력 | bg white / border slate-300 / text slate-700 | bg slate-50 | bg slate-100 | opacity-50 |
| Danger | 삭제, 초기화 | bg white / border red-300 / text red-600 | bg red-50 | bg red-100 | opacity-50 |
| Ghost | 모달 닫기(✕), 취소 | 투명 / text slate-500 | bg slate-100 | bg slate-200 | — |

**공통:** `transition` 150ms, 포커스 시 `ring-2 ring-emerald-400 ring-offset-2` (키보드 사용자에게 현재 위치를 명확히 표시).

### 3.2 입력 필드

| 상태 | 스타일 | 발생 조건 |
|---|---|---|
| Default | bg white / border slate-300 / rounded / px-2 py-2 / text-sm | 평상시 |
| Focus | border emerald-500 / ring-2 ring-emerald-200 | 클릭·탭 이동 |
| **Error** | **border red-500 / bg red-50** + 하단 오류 문구 | 음수·문자·범위 초과 (FN-06) |
| Warning | border amber-400 | 권장 범위 밖이나 계산은 유효 |
| Disabled | bg slate-100 / text slate-400 | 밀도(7.85) 등 고정값 |
| Readonly (단가 표기) | 테두리 없음 / text-xs / text slate-400 | 마스터 연동 표시 |

**숫자 입력 공통:** `type="number"`, `inputmode="decimal"`, 우측 정렬, 단위는 필드 밖 라벨에 표기(값과 섞이지 않게).

### 3.3 카드 / 패널

| 요소 | 스타일 |
|---|---|
| 입력 섹션 카드 | bg slate-50 / border slate-200 / rounded-lg / p-4 / 섹션 간 간격 24px |
| 결과 패널 | bg slate-900 / rounded-lg / p-6 / sticky top-6 |
| 최종가 영역 | 상단 `border-t-2 border-dashed border-slate-700` / pt-6 |
| 모달 | bg white / rounded-xl / shadow-2xl / max-w-2xl / 배경 딤 `bg-black/50` |
| 앱 컨테이너 | max-w-7xl / mx-auto / p-6 |

### 3.4 경고 표시

```
┌─────────────────────────────────────────────┐
│ ⚠  마진율이 음수입니다. 원가 이하 견적입니다.  │  amber 계열
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│ ⛔  최종 견적가가 0원 이하입니다.              │  red 계열
└─────────────────────────────────────────────┘
```
- 배경 `amber-50` / `red-50`, 좌측 4px 컬러 바, 아이콘 + 문장.
- 경고 0건이면 영역 자체를 숨긴다(빈 박스를 남기지 않는다).

### 3.5 아이콘
별도 아이콘 라이브러리를 추가하지 않고 **유니코드 이모지**를 사용한다: 🛠 ⚙ 🗂 🖨 💾 📊 ⚠ ⛔ ✅ ✕

> **왜 라이브러리를 안 쓰는가** — 아이콘 10개를 위해 수백 KB의 폰트나 SVG 스프라이트를 CDN에서 더 받는 것은 REQ-16(단일 파일)과 오프라인 대응에 부담만 준다. 이모지는 이미 OS에 있다.

### 3.6 토스트

| 항목 | 값 |
|---|---|
| 위치 | 우측 하단 24px 여백 |
| 크기 | 최소 폭 280px, px-4 py-3, rounded-lg, shadow-lg |
| 색 | 성공 emerald-600 / 경고 amber-600 / 오류 red-600, 흰 텍스트 |
| 지속 | 3초 후 자동 소멸 (fade 200ms) |
| 동시 표시 | 최대 3개, 위로 쌓임 |

---

## 4. 화면별 디자인 시안

### [SCR-01] 견적 시뮬레이터
| 영역 | 적용 |
|---|---|
| 페이지 | bg slate-100, 컨테이너 max-w-7xl, 카드 bg white + rounded-xl + shadow |
| 헤더 | 하단 border slate-200, 제목 24px Bold, 우측 Secondary 버튼 3개 |
| 좌측 입력 | 섹션 카드 4개 세로 배치, 섹션 제목 18px SemiBold slate-700 |
| 우측 결과 | slate-900 패널, sticky, 제목 teal-400 |
| 결과 항목 | 라벨 slate-400 좌측 / 금액 monospace slate-100 우측 정렬 |
| 소계 | amber-300, 상단 `border-t border-slate-700` |
| 최종가 | emerald-500, 30px Black, monospace |
| 주 버튼 | 패널 하단 전폭 emerald-500 |
| 라이트/다크 | 라이트 전용 (1.3장) |

### [SCR-02] 마스터 설정 모달
| 영역 | 적용 |
|---|---|
| 모달 | white, rounded-xl, max-w-2xl, 최대 높이 80vh 내부 스크롤 |
| 탭 | 선택 시 하단 2px emerald-500 + text slate-900 / 비선택 text slate-500 |
| 안내문 | bg slate-50, text-xs slate-500, ⓘ 아이콘 |
| 하단 버튼 바 | 좌측 Danger(초기화), 우측 Ghost(취소) + Primary(저장하고 적용) |

### [SCR-03] 견적 이력 모달
| 영역 | 적용 |
|---|---|
| 목록 | 행 hover 시 bg slate-50, 행 간 border slate-200 |
| 최종가 | monospace, 우측 정렬, slate-800 SemiBold |
| 행 액션 | Secondary(불러오기) + Danger(삭제), 작은 크기 |
| 빈 상태 | 중앙 정렬 slate-400 "저장된 견적이 없습니다" |

### [SCR-04] 견적서 출력 뷰 (인쇄)
| 영역 | 적용 |
|---|---|
| 배경 | **전부 흰색.** 화면용 어두운 패널 색을 인쇄로 가져가지 않는다 |
| 제목 | "견 적 서" 28px Bold, 자간 넓게, 중앙 정렬 |
| 표 헤더 | bg slate-900 / 흰 텍스트 (인쇄 시에도 유지 — 항목 구분용) |
| 표 본문 | 홀짝 행 bg white / slate-50, border slate-300 |
| 금액 | monospace, 우측 정렬 |
| 합계 행 | 상단 2px 실선, 16px Bold |
| 고지 문구 | 10px slate-600, 하단 고정 |
| 여백 | `@page { size: A4; margin: 15mm; }` |

**인쇄 전용 CSS 핵심:**
```css
@media print {
  .no-print { display: none !important; }
  #printArea { display: block !important; }
  body { background: #fff; }
  * { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  @page { size: A4; margin: 15mm; }
}
```

---

## 5. 접근성 체크

| 항목 | 기준 | 적용 |
|---|---|---|
| 색 대비 | AA 4.5:1 이상 | 1.2장 전 조합 검증 완료 |
| 색 의존 금지 | 색 외 신호 제공 | 경고에 아이콘+텍스트 병기 |
| 라벨 연결 | 모든 입력에 `label for` | 스크린리더가 필드 용도를 읽음 |
| 포커스 표시 | 명확한 링 | `ring-2 ring-emerald-400` |
| 동적 변경 알림 | `aria-live="polite"` | 결과 패널에 적용 (계산 결과 변경을 읽어줌) |
| 키보드 조작 | 마우스 없이 전 기능 | Tab 순서 논리적, 모달 ESC 닫기 |
| 터치 영역 | 최소 40×40px | 버튼 `py-2` 이상 |

---

## 6. 완료 체크리스트

- [x] 컬러 팔레트와 타이포그래피가 정의되었는가 → 1.2 / 2장
- [x] 컴포넌트 스타일 가이드가 작성되었는가 → 3장 (버튼·입력·카드·경고·토스트)
- [x] 주요 화면의 디자인 시안이 완성되었는가 → 4장 (SCR-01~04)
- [x] 반응형/다크모드 대응이 고려되었는가 → 4번 문서 3장 / 본 문서 1.3장(미지원 결정 및 근거)
- [x] 색 대비가 접근성 기준을 만족하는가 → 1.2장 검증표
