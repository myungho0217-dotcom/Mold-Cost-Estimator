# 사출금형 견적 시뮬레이터 기능 명세서 (PRD/FS) V1.0

> **PRD/FS** (Product Requirement Document / Function Specification) — "어떻게 동작하는가"를 확정하는 문서. 개발자가 이 문서만 보고 구현할 수 있어야 한다.

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-09-18 |
| 버전 | V1.0 |
| 선행 문서 | `1.금형견적_요구사항명세서_SRS.md` |

---

## 1. 기능 목록 총괄

| ID | 기능명 | 설명 | 우선순위 | 관련 REQ | Phase |
|---|---|---|---|---|---|
| FN-01 | 재료비 산출 | 치수·강재로 중량과 재료비 계산 | High | REQ-01, REQ-12 | 1 |
| FN-02 | 가공비 산출 | 공정시간 × 임율 × 복잡도 × 캐비티 계수 | High | REQ-02, REQ-06, REQ-07 | 1→2 |
| FN-03 | 기구비 산출 | 슬라이드·경사핀 수량 × 단가 | High | REQ-03 | 1 |
| FN-04 | 최종가 산출 | 제조원가 + 마진 + 일반관리비 | High | REQ-04 | 1 |
| FN-05 | 실시간 재계산 | 입력 변경 즉시 전체 재계산 및 바인딩 | High | REQ-05 | 1 |
| FN-06 | 입력 검증 | 비정상 입력 방어 및 경고 표시 | High | REQ-15 | 1 |
| FN-07 | 마스터 데이터 관리 | 단가·임율 조회/수정/저장/초기화 | High | REQ-08, REQ-18 | 1→4 |
| FN-08 | 견적서 출력 | 인쇄 레이아웃 생성 및 브라우저 인쇄 호출 | High | REQ-09, REQ-19 | 3 |
| FN-09 | 견적 이력 관리 | 저장·목록·불러오기·삭제 | Medium | REQ-10 | 4 |
| FN-10 | 마스터 백업 | JSON 내보내기/가져오기 | Low | REQ-11 | 4 |
| FN-11 | 자가검증 | 콘솔에서 기대값 대조 실행 | Medium | KPI-02 | 1 |

---

## 2. 공통 데이터 정의

### 2.1 마스터 데이터 (MasterData)

```js
const DEFAULT_MASTER = {
  steel: {                          // 강재 kg당 단가 (원/kg)
    'P20':    3500,
    'NAK80':  5500,
    'H13':    8000
  },
  rate: {                           // 공정 시간당 임율 (원/hr)
    design:   35000,                // 설계/CAD
    cnc:      50000,                // CNC 고속가공
    edm:      40000,                // 방전가공
    assembly: 30000                 // 사상 및 조립
  },
  density:        7.85,             // 철 비중 (g/cm3) — 상수, 편집 불가
  moldBaseFactor: 1.2,              // 몰드베이스 가산 계수 (L×W×factor)
  slideUnit:      1200000,          // 슬라이드 코어 1개당 (원)
  lifterUnit:      600000,          // 경사핀 1개당 (원)
  trialRun:        500000,          // 시사출 고정비 (원)
  overhead:             0,          // 일반관리비 (원) — D-03
  cavityFactor:      0.15,          // 캐비티 증가분 계수 — D-02
  complexity: {                     // 형상 복잡도 가중치 — REQ-06
    simple:    1.0,                 // 일반
    complex:   1.2,                 // 복잡
    precision: 1.5                  // 초정밀
  },
  hotRunner: {                      // 핫러너 옵션 (원)
    'none':   0,
    '1drop':  2500000,
    '2drop':  4500000
  },
  company: {                        // 출력물 공급자 정보
    name: '', address: '', tel: '', manager: ''
  }
};
```

> ⚠️ **단일 소스 원칙 (REQ-08 / plan.md R7)** — 화면 라벨의 "임율: 50,000원" 같은 숫자 표기는 **반드시 이 객체에서 렌더링**한다. HTML에 숫자를 직접 적으면 단가 변경 시 두 곳을 고쳐야 하고, 한쪽을 잊으면 화면 표기와 계산 결과가 어긋난다.

### 2.2 견적 입력 (QuoteInput)

| 필드 | 타입 | 필수 | 기본값 | 범위 | 설명 |
|---|---|---|---|---|---|
| customer | string | N | "" | 0~50자 | 고객사명 |
| project | string | N | "" | 0~50자 | 프로젝트/품명 |
| quoteDate | date | Y | 오늘 | — | 견적일 |
| validDays | number | Y | 30 | 1~365 | 견적 유효기간(일) |
| length | number | Y | 400 | 0~2000 | 가로 L (mm) |
| width | number | Y | 400 | 0~2000 | 세로 W (mm) |
| thickness | number | Y | 350 | 0~2000 | 두께 T (mm) |
| steelType | enum | Y | NAK80 | master.steel 키 | 강재 종류 |
| cavities | number | Y | 2 | 1~64 | 캐비티 수 |
| tDesign | number | Y | 20 | 0~2000 | 설계 시간 (hr) |
| tCnc | number | Y | 45 | 0~2000 | CNC 시간 (hr) |
| tEdm | number | Y | 30 | 0~2000 | 방전 시간 (hr) |
| tAssembly | number | Y | 25 | 0~2000 | 사상/조립 시간 (hr) |
| complexity | enum | Y | simple | simple/complex/precision | 형상 복잡도 |
| slideCount | number | Y | 1 | 0~32 | 슬라이드 개수 |
| lifterCount | number | Y | 2 | 0~64 | 경사핀 개수 |
| hotRunner | enum | Y | none | master.hotRunner 키 | 핫러너 옵션 |
| marginRate | number | Y | 25 | 0~100 | 목표 마진율 (%) |

### 2.3 견적 결과 (QuoteResult)

| 필드 | 타입 | 설명 |
|---|---|---|
| weight | number | 강재 예상 중량 (kg, 소수 1자리) |
| material | number | 재료비 합계 (원, 정수) |
| machining | number | 가공 공정비 (원, 정수) |
| mechanism | number | 기구 추가비 (원, 정수) |
| trialRun | number | 시사출비 (원, 정수) |
| totalCost | number | 제조원가 (원, 정수) |
| marginProfit | number | 마진 금액 (원, 정수) |
| overhead | number | 일반관리비 (원, 정수) |
| finalPrice | number | 최종 공급가 (원, 정수, 부가세 별도) |
| warnings | string[] | 검증 경고 메시지 목록 |

**반올림 규칙:** 중량은 소수 1자리, 모든 금액은 각 항목 산출 직후 `Math.round()`. 최종가만 마지막에 반올림하면 항목 합계와 총액이 1원 단위로 어긋날 수 있으므로 **항목별 반올림 후 합산**한다.

---

## 3. 기능 상세 명세

### [FN-01] 재료비 산출

- **설명:** 금형 외형 치수와 강재 종류로 강재 중량과 재료비 총액을 계산한다.
- **입력:** length, width, thickness, steelType, hotRunner
- **처리 로직:**
  1. `weight = L × W × T ÷ 1,000,000 × master.density`
  2. `steelCost = round(weight × master.steel[steelType])`
  3. `moldBaseCost = round(L × W × master.moldBaseFactor)`
  4. `hotRunnerCost = master.hotRunner[hotRunner]`
  5. `material = steelCost + moldBaseCost + hotRunnerCost`
- **출력:** weight(kg), material(원)
- **업무 규칙:**
  - R1-1: 밀도 7.85는 철 기준 고정 상수이며 화면에서 편집하지 않는다.
  - R1-2: 몰드베이스비는 투영 면적(L×W) 비례 간이 추정식이다. **정밀 견적에서는 실제 몰드베이스 견적가로 대체해야 하며**, 그때까지는 `moldBaseFactor`를 마스터에서 조정해 보정한다.
  - R1-3: 중량은 외형 직육면체 기준 총중량이며 포켓 가공에 따른 실제 감량은 반영하지 않는다(과대 추정 = 안전측).
- **예외 처리:**
  - E1-1: 치수 중 하나라도 0 → weight 0, material은 핫러너비만 계상. 오류 없이 진행.
  - E1-2: 치수 2000mm 초과 → 경고 "일반 금형 범위를 초과한 치수입니다" 표시, 계산은 계속.
- **데이터 요구사항:** 입력 number 3 + enum 2 / 저장 없음(파생값)
- **권한:** 전체 사용자
- **완료 기준:** 400×400×350, NAK80 입력 시 중량 **439.6kg** 표시

---

### [FN-02] 가공비 산출

- **설명:** 공정별 소요 시간에 임율을 곱하고 형상 복잡도와 캐비티 수 가중치를 적용한다.
- **입력:** tDesign, tCnc, tEdm, tAssembly, complexity, cavities
- **처리 로직:**
  1. `baseMachining = tDesign×rate.design + tCnc×rate.cnc + tEdm×rate.edm + tAssembly×rate.assembly`
  2. `cx = master.complexity[complexity]`
  3. `cavityMul = 1 + (cavities - 1) × master.cavityFactor`
  4. `machining = round(baseMachining × cx × cavityMul)`
- **출력:** machining(원)
- **업무 규칙:**
  - R2-1: 복잡도 가중치는 **가공비에만** 적용한다. 재료비·기구비는 영향받지 않는다.
  - R2-2: 캐비티 계수는 설계 시간에도 동일 적용한다(다수 캐비티는 레이아웃 설계 공수도 증가).
  - R2-3: 캐비티 1개일 때 `cavityMul = 1.0` (가중 없음).
- **예외 처리:**
  - E2-1: 모든 시간 0 → machining 0. 경고 "가공 시간이 입력되지 않았습니다" 표시.
  - E2-2: cavities < 1 → 1로 클램프.
- **완료 기준:** 복잡도를 일반(1.0)→초정밀(1.5)로 변경 시 **가공비만 정확히 1.5배**가 되고 재료비·기구비 불변

---

### [FN-03] 기구비 산출

- **설명:** 언더컷 처리 기구 수량에 개당 표준 단가를 곱한다.
- **입력:** slideCount, lifterCount
- **처리 로직:** `mechanism = slideCount × master.slideUnit + lifterCount × master.lifterUnit`
- **출력:** mechanism(원)
- **업무 규칙:** R3-1: 기구 단가는 가공 공수·부품비를 포함한 개당 총액이며 복잡도 가중을 중복 적용하지 않는다.
- **예외 처리:** E3-1: 음수 입력 → 0으로 클램프 후 경고
- **완료 기준:** 슬라이드 1, 경사핀 2 → 2,400,000원

---

### [FN-04] 최종가 산출

- **설명:** 제조원가에 마진율과 일반관리비를 적용해 공급가를 확정한다.
- **입력:** FN-01~03 결과, marginRate
- **처리 로직:**
  1. `totalCost = material + machining + mechanism + master.trialRun`
  2. `marginProfit = round(totalCost × marginRate ÷ 100)`
  3. `finalPrice = totalCost + marginProfit + master.overhead`
- **출력:** totalCost, marginProfit, finalPrice
- **업무 규칙:**
  - R4-1: 최종 공급가는 **부가세 별도** 금액이다. 화면과 출력물에 항상 명시한다.
  - R4-2: 일반관리비는 마진 계산 **이후** 가산한다(마진 대상이 아닌 고정 경비).
  - R4-3: 시사출비는 제조원가에 포함되어 마진 대상에 들어간다.
- **예외 처리:**
  - E4-1: marginRate < 0 → 경고 "마진율이 음수입니다. 원가 이하 견적이 됩니다", 계산은 수행.
  - E4-2: marginRate > 100 → 경고 "마진율이 100%를 초과합니다", 계산은 수행.
  - E4-3: finalPrice ≤ 0 → 결과 영역에 적색 경고 배지 표시.
- **완료 기준:** 손계산 대조표와 오차 0원

---

### [FN-05] 실시간 재계산

- **설명:** 입력값이 바뀌면 조회 버튼 없이 전체 계산과 화면 바인딩을 수행한다.
- **입력:** 입력 요소의 `input` / `change` 이벤트
- **처리 로직:**
  1. 입력 컨테이너에 **이벤트 위임** 1개 등록 (요소마다 `oninput` 속성을 다는 원문 방식은 요소 추가 시 누락 위험)
  2. `readInputs()` → `validate()` → `calculate()` → `render()` 순차 실행
  3. `render()`는 결과 필드 9개와 경고 영역을 갱신
- **출력:** 결과 패널 전체 갱신
- **업무 규칙:**
  - R5-1: 숫자는 `toLocaleString` 한국 로케일로 천 단위 구분
  - R5-2: 페이지 최초 로드 시 1회 자동 실행
- **예외 처리:** E5-1: 계산 중 예외 발생 시 결과를 "—"로 표시하고 콘솔에 스택 출력. `try/catch`로 감싸 화면 전체가 멈추지 않게 한다.
- **완료 기준:** 입력 변경 후 100ms 내 화면 반영, 콘솔 오류 0건

---

### [FN-06] 입력 검증

- **설명:** 비정상 입력이 계산을 오염시키거나 앱을 중단시키지 않도록 방어한다.
- **입력:** QuoteInput 전체
- **처리 로직:**
  1. 숫자 파싱 결과가 `NaN`이면 0으로 대체
  2. 2.2장 범위 표 기준으로 `clamp(value, min, max)`
  3. 클램프가 발생한 필드는 경고를 `warnings`에 추가
  4. 해당 입력 요소에 오류 테두리 스타일 적용
- **출력:** 정규화된 입력값 + warnings[]
- **업무 규칙:**
  - R6-1: **입력값을 사용자 모르게 조용히 바꾸지 않는다.** 클램프했으면 반드시 알린다.
  - R6-2: 검증 실패가 계산 중단으로 이어지지 않는다(방어 후 계속 진행).
- **예외 처리:**

  | 입력 | 처리 | 사용자 알림 |
  |---|---|---|
  | 빈 값 | 0 | 없음 (입력 중일 수 있음) |
  | 문자열 | 0 | "숫자를 입력하세요" |
  | 음수 | 0 | "음수는 0으로 처리됩니다" |
  | 상한 초과 | 상한값 | "최대 {max}까지 입력 가능합니다" |

- **완료 기준:** 모든 입력 필드에 음수·문자·공백을 넣어도 콘솔 오류 0건

---

### [FN-07] 마스터 데이터 관리

- **설명:** 단가·임율을 화면에서 수정하고 브라우저에 영구 저장한다.
- **입력:** 마스터 편집 패널의 각 필드
- **처리 로직:**
  1. 로드 시 `localStorage`의 `mold_master_v1` 조회
  2. 없거나 파싱 실패 → `DEFAULT_MASTER` 사용
  3. 있으면 `{...DEFAULT_MASTER, ...저장값}`으로 **병합** (앱 버전 업 시 새 키 누락 방지)
  4. 저장 버튼 → 검증 후 저장, 성공 토스트, 전체 재계산
  5. 초기화 버튼 → 확인 대화 후 기본값 복원
- **출력:** 갱신된 마스터, 재계산된 견적
- **업무 규칙:**
  - R7-1: 마스터 변경은 **현재 화면의 견적에 즉시 반영**되며, 저장된 과거 이력의 금액은 변경하지 않는다(이력은 당시 확정 금액을 보존).
  - R7-2: 단가는 0 이상만 허용. 음수 저장 차단.
  - R7-3: 저장 실패(용량 초과·시크릿 모드)는 오류 토스트를 띄우고 메모리 값으로만 계속 동작한다.
- **예외 처리:**
  - E7-1: localStorage 접근 불가 → 경고 후 세션 한정 동작
  - E7-2: 저장된 JSON 손상 → 기본값 복원 + "저장된 설정을 읽을 수 없어 기본값으로 복원했습니다"
- **권한:** 전체 사용자 (실무상 공장장/대표가 설정)
- **완료 기준:** CNC 임율 55,000 변경 → 저장 → 브라우저 재시작 → 값 유지 및 가공비 반영

---

### [FN-08] 견적서 출력

- **설명:** 확정 견적을 A4 견적서로 구성하고 브라우저 인쇄를 호출한다. (**D-01: `window.print()` 방식**)
- **입력:** QuoteInput + QuoteResult + master.company
- **처리 로직:**
  1. `#printArea`에 견적서 DOM 구성(헤더·공급자·수요자·내역표·합계·고지문·서명란)
  2. `@media print`로 화면 UI(`.no-print`)를 숨기고 `#printArea`만 노출
  3. `window.print()` 호출
  4. 사용자가 인쇄 대화상자에서 "PDF로 저장" 또는 실제 인쇄 선택
- **출력:** A4 1페이지 견적서
- **업무 규칙:**
  - R8-1: 출력물 금액은 화면과 **동일한 결과 객체**를 바인딩한다. 재계산하지 않는다.
  - R8-2: 하단 고지 문구 고정 — **"본 견적은 표준 산출식에 의한 참고용 추정 견적이며, 최종 계약가와 다를 수 있습니다. 금액은 부가가치세 별도입니다."** (REQ-19)
  - R8-3: 견적일·유효기간·문서번호를 헤더에 표기. 문서번호 형식 `Q-YYYYMMDD-NN`.
  - R8-4: 공급자 정보(상호·주소·연락처·담당자)는 마스터에 포함해 편집 가능하게 한다.
- **예외 처리:**
  - E8-1: 경고가 있는 견적은 출력 전 확인 대화 "경고가 있는 견적입니다. 계속하시겠습니까?"
  - E8-2: 고객사명이 비어 있으면 "고객사명을 입력하세요" 안내 후 출력 중단
- **완료 기준:** 출력물 7개 금액 항목이 화면과 100% 일치, 한글 깨짐 없음

---

### [FN-09] 견적 이력 관리

- **설명:** 견적을 로컬에 저장하고 목록에서 재사용한다.
- **처리 로직:**
  1. 저장: `{id, savedAt, customer, project, input, result}`를 `mold_history_v1` 배열 맨 앞에 추가
  2. 50건 초과 시 가장 오래된 항목 제거 (FIFO)
  3. 목록: 저장일시·고객사·프로젝트·최종가, 최신순
  4. 불러오기: 해당 `input`을 화면에 복원 후 **현재 마스터로 재계산**
  5. 삭제: 확인 후 해당 id 제거
- **업무 규칙:**
  - R9-1: 불러온 견적은 현재 마스터로 재계산되므로 **저장 당시 금액과 다를 수 있다.** 차이가 있으면 "저장 당시 최종가 X원 / 현재 기준 Y원"을 함께 표시한다.
  - R9-2: 이력 삭제는 되돌릴 수 없으므로 반드시 확인 대화를 거친다.
- **예외 처리:** E9-1: 저장 용량 초과 → 오래된 항목부터 제거 후 재시도, 그래도 실패하면 오류 안내
- **완료 기준:** 저장 → 브라우저 재시작 → 목록 존재 → 불러오기 시 입력값 전부 복원

---

### [FN-10] 마스터 백업

- **처리 로직:** 내보내기는 마스터를 JSON Blob으로 만들어 다운로드 링크 클릭 유발. 가져오기는 파일 입력으로 읽어 파싱·검증 후 병합 저장.
- **업무 규칙:** R10-1: 가져오기 전 현재 설정을 덮어쓴다는 확인 대화 필수. R10-2: 알 수 없는 키는 무시하고 스키마에 있는 키만 반영한다.
- **완료 기준:** 내보낸 파일을 다른 PC에서 가져와 동일 단가 적용

---

### [FN-11] 자가검증

- **설명:** 테스트 프레임워크 없이 회귀를 확인하는 콘솔 함수.
- **처리 로직:** `__selfTest()` 실행 시 고정 입력 3세트에 대해 기대값과 실제값을 비교하고 표로 콘솔 출력, 전체 통과 여부 반환.
- **업무 규칙:** R11-1: 기대값은 7번 문서의 손계산 결과를 기준으로 한다. R11-2: 마스터가 기본값이 아니면 "기본 마스터가 아니므로 결과가 다를 수 있음" 경고 출력.
- **완료 기준:** 코드 수정 후 `__selfTest()` 한 번으로 계산 회귀 여부 판정

---

## 4. 사용자 흐름 (User Flow)

### 4.1 주 흐름 — 신규 견적 작성
```
앱 실행
  → [기본값으로 초기 계산 표시]
  → 고객사·프로젝트 입력
  → 금형 치수·강재 입력       ─┐
  → 공정 시간 입력             ├→ 입력마다 즉시 재계산 (FN-05)
  → 복잡도·기구 수량 입력       │
  → 마진율 조정               ─┘
  → [결과 확인]
      ├─ 경고 있음 → 입력 수정으로 복귀
      └─ 이상 없음 ↓
  → [견적 저장] (FN-09)
  → [견적서 출력] (FN-08)
  → 인쇄 대화상자 → "PDF로 저장" 선택
  → 완료
```

### 4.2 보조 흐름 — 단가 갱신
```
앱 실행 → [마스터 설정] 열기 → 임율/단가 수정
  → 검증 통과? ─ No → 오류 표시 → 수정으로 복귀
                └ Yes → 저장 → 토스트 → 현재 견적 자동 재계산 → 패널 닫기
```

### 4.3 보조 흐름 — 과거 견적 재사용
```
앱 실행 → [이력] 열기 → 목록에서 선택 → 불러오기
  → 입력값 복원 + 현재 마스터로 재계산
  → 금액 차이 있으면 비교 표시 → 수정 → 저장/출력
```

---

## 5. 비기능 요구사항 상세

| 구분 | 기준 | 검증 방법 |
|---|---|---|
| 성능 | 입력→화면 갱신 100ms 이내 (산술 연산 수십 회 수준으로 병목 없음) | 체감 + 타이머 측정 |
| 성능 | 초기 로드 3초 이내 (CDN 캐시 후 1초) | Network 탭 |
| 보안 | 인증 없음(단일 PC 전제). **외부 전송 0건** | Network 탭에 CDN 외 요청 없음 |
| 보안 | API 키·시크릿이 존재하지 않는 구조 | 소스 검색 |
| 개인정보 | 고객사명은 로컬에만 저장. 외부 전송·수집 없음 | 설계상 보장 |
| 호환성 | Chrome / Edge 최신 2개 버전 | 수동 확인 |
| 호환성 | ES6+ 문법 사용. IE 미지원(단종) | — |
| 접근성 | 모든 입력에 `label for` 연결, 결과 영역에 `aria-live="polite"` | 키보드 탭 이동 확인 |
| 접근성 | 경고는 색상만으로 구분하지 않고 아이콘·텍스트 병기 | 육안 확인 |

---

## 6. 완료 체크리스트

- [x] 모든 요구사항(REQ)이 기능(FN)으로 분해되었는가 → 1장 매핑표 (REQ-01~19 → FN-01~11)
- [x] 각 기능의 입력/처리/출력/예외가 정의되었는가 → 3장
- [x] 업무 규칙과 권한이 명시되었는가 → 각 기능 R·권한 항목
- [x] 사용자 흐름이 작성되었는가 → 4장
