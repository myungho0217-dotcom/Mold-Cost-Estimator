# 사출금형 견적 시뮬레이터 정보구조도 (IA) V1.0

> **IA** (Information Architecture, 정보구조도) — 화면이 몇 개이고 서로 어떻게 연결되며 데이터가 어디에 어떤 모양으로 저장되는지 확정하는 문서.

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-09-18 |
| 버전 | V1.0 |
| 선행 문서 | `2.금형견적_기능명세서_PRD.md` |

---

## 1. 구조 원칙

단일 HTML 파일이므로 **페이지 이동(URL 라우팅)이 없다.** 모든 화면은 하나의 문서 안에서 표시/숨김으로 전환한다.

| 구조 결정 | 내용 | 근거 |
|---|---|---|
| 페이지 수 | 물리적 페이지 1개 (`index.html`) | REQ-16 단일 파일 |
| 화면 전환 | 메인은 상시 표시, 부가 화면은 모달 오버레이 | 견적 작업 흐름이 끊기지 않아야 함 |
| 라우팅 | 없음. 해시(`#`)도 사용하지 않음 | 뒤로가기로 입력값이 날아가는 사고 방지 |
| 메뉴 체계 | 헤더 우측 버튼 3개 (설정 · 이력 · 출력) | 2depth 불필요 |

> 💡 **왜 모달인가** — 견적 담당자는 치수를 입력하다가 단가를 확인하고 다시 돌아온다. 별도 페이지로 이동하면 입력 중인 값을 잃을 위험이 있다. 모달은 메인 상태를 그대로 유지한다.

---

## 2. 사이트맵

```
index.html (단일 문서)
│
├── [상시] 헤더 — 앱 제목 / 버튼 3개
│
├── [상시] SCR-01 견적 시뮬레이터 (메인)
│   ├── 섹션 A: 견적 정보 (고객사·프로젝트·견적일·유효기간)
│   ├── 섹션 B: 금형 사이즈 및 강재
│   ├── 섹션 C: 공정별 소요 시간
│   ├── 섹션 D: 기구물 및 비즈니스 조건
│   └── 섹션 E: 실시간 견적 결과 패널 (우측 고정)
│
├── [모달] SCR-02 마스터 단가 설정
│   ├── 탭 1: 강재 단가
│   ├── 탭 2: 공정 임율
│   ├── 탭 3: 기구·옵션·계수
│   └── 탭 4: 공급자 정보 / 백업
│
├── [모달] SCR-03 견적 이력
│   └── 이력 목록 → 불러오기 / 삭제
│
└── [인쇄 전용] SCR-04 견적서 출력 뷰
    (화면에서는 숨김, @media print에서만 표시)
```

---

## 3. 화면 목록

| ID | 화면명 | 표시 방식 | 접근 경로 | 접근 권한 | 주요 기능 | Phase |
|---|---|---|---|---|---|---|
| SCR-01 | 견적 시뮬레이터 | 상시 표시 | 앱 실행 즉시 | 전체 | FN-01~06 (입력·계산·결과) | 1 |
| SCR-02 | 마스터 단가 설정 | 모달 | 헤더 [⚙ 단가설정] | 전체 | FN-07, FN-10 | 1→4 |
| SCR-03 | 견적 이력 | 모달 | 헤더 [🗂 이력] | 전체 | FN-09 | 4 |
| SCR-04 | 견적서 출력 뷰 | 인쇄 전용 | 헤더/결과패널 [🖨 견적서 출력] | 전체 | FN-08 | 3 |
| SCR-05 | 토스트 알림 | 오버레이 | 시스템 자동 | — | 저장 성공/실패 피드백 | 1 |
| SCR-06 | 확인 대화 | 오버레이 | 시스템 자동 | — | 삭제·초기화·덮어쓰기 확인 | 4 |

> **권한 컬럼이 전부 "전체"인 이유** — SRS 3.3장에서 로그인·권한 관리를 제외 범위로 확정했다. 단일 사내 PC에서 견적 담당자와 공장장이 같은 화면을 쓴다. 권한 계층을 두면 관리 대상만 늘고 보호되는 자산이 없다.

---

## 4. 화면 간 이동 경로 (Navigation Flow)

```
                   ┌──────────────────────────┐
                   │   SCR-01 견적 시뮬레이터   │  ← 모든 흐름의 기준점
                   └──┬────────┬────────┬─────┘
          [⚙ 단가설정] │        │        │ [🖨 견적서 출력]
                      ↓        │        ↓
          ┌───────────────┐    │   ┌──────────────────┐
          │ SCR-02 마스터  │    │   │ SCR-04 출력 뷰    │
          │   단가 설정    │    │   │ → 브라우저 인쇄    │
          └───┬───────┬───┘    │   └────────┬─────────┘
       [저장] │       │ [닫기]  │            │ (대화상자 종료)
              ↓       └────────┼────────────┘
      재계산 후 복귀            │
                     [🗂 이력] │
                              ↓
                   ┌──────────────────┐
                   │ SCR-03 견적 이력  │
                   └───┬──────────┬───┘
            [불러오기] │          │ [삭제]
                       ↓          ↓
             입력값 복원+재계산   SCR-06 확인 → 목록 갱신
                   후 SCR-01 복귀
```

### 4.1 이동 규칙

| 규칙 | 내용 |
|---|---|
| N-1 | 모달은 동시에 하나만 열린다. 열려 있으면 기존 모달을 닫고 전환한다. |
| N-2 | 모달은 `ESC` 키, 배경 클릭, 닫기 버튼 3가지 방법으로 닫힌다. |
| N-3 | 모달을 닫을 때 SCR-01의 입력값은 절대 초기화하지 않는다. |
| N-4 | SCR-02에서 저장하면 모달을 닫고 SCR-01을 즉시 재계산한다. |
| N-5 | SCR-04는 화면 전환이 아니라 인쇄 매체 전환이다. 인쇄 대화상자를 닫으면 SCR-01이 그대로 남아 있다. |
| N-6 | 브라우저 뒤로가기는 사용하지 않는다(라우팅 없음). 새로고침 시 입력값은 초기화되고 마스터·이력만 유지된다. |

---

## 5. 데이터 구조

### 5.1 저장소 개요

| 저장소 키 | 내용 | 수명 | 최대 크기 |
|---|---|---|---|
| `mold_master_v1` | 마스터 단가·임율·계수·공급자 정보 | 영구 (사용자가 초기화할 때까지) | 약 2KB |
| `mold_history_v1` | 견적 이력 배열 (최근 50건) | 영구 (FIFO 자동 정리) | 약 200KB |
| (메모리) `currentInput` | 현재 입력값 | 세션 (새로고침 시 소멸) | — |
| (메모리) `currentResult` | 현재 계산 결과 | 세션 | — |

> ⚠️ localStorage 총 용량은 도메인당 약 5MB다. `file://` 로 직접 실행할 때는 브라우저·경로별로 저장소가 분리되므로 **파일을 다른 폴더로 옮기면 저장된 마스터·이력이 보이지 않을 수 있다.** 이 리스크는 FN-10(JSON 내보내기)으로 완화한다.

### 5.2 엔티티: MasterData (`mold_master_v1`)

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|---|---|---|---|---|
| steel | object | Y | P20/NAK80/H13 | 강재별 kg당 단가 (원) |
| rate.design | number | Y | 35000 | 설계 임율 (원/hr) |
| rate.cnc | number | Y | 50000 | CNC 임율 (원/hr) |
| rate.edm | number | Y | 40000 | 방전 임율 (원/hr) |
| rate.assembly | number | Y | 30000 | 사상·조립 임율 (원/hr) |
| density | number | Y | 7.85 | 철 비중 (편집 불가) |
| moldBaseFactor | number | Y | 1.2 | 몰드베이스 가산 계수 |
| slideUnit | number | Y | 1200000 | 슬라이드 개당 단가 (원) |
| lifterUnit | number | Y | 600000 | 경사핀 개당 단가 (원) |
| trialRun | number | Y | 500000 | 시사출 고정비 (원) |
| overhead | number | Y | 0 | 일반관리비 (원) |
| cavityFactor | number | Y | 0.15 | 캐비티 증가 계수 |
| complexity | object | Y | 1.0/1.2/1.5 | 복잡도 가중치 |
| hotRunner | object | Y | 0/2.5M/4.5M | 핫러너 옵션 금액 (원) |
| company.name | string | N | "" | 공급자 상호 |
| company.address | string | N | "" | 공급자 주소 |
| company.tel | string | N | "" | 공급자 연락처 |
| company.manager | string | N | "" | 담당자명 |
| schemaVersion | number | Y | 1 | 스키마 버전 (마이그레이션 판단용) |

### 5.3 엔티티: QuoteHistoryItem (`mold_history_v1[]`)

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| id | string | Y | 고유 ID (저장 시각 기반) |
| savedAt | string | Y | 저장 일시 (ISO 8601) |
| docNo | string | Y | 문서번호 `Q-YYYYMMDD-NN` |
| customer | string | N | 고객사명 (목록 표시용 비정규화) |
| project | string | N | 프로젝트명 (목록 표시용 비정규화) |
| input | object | Y | QuoteInput 전체 스냅샷 |
| result | object | Y | QuoteResult 전체 스냅샷 (당시 확정 금액) |

> **`result`를 함께 저장하는 이유** — 마스터 단가가 나중에 오르면 같은 입력값이라도 금액이 달라진다. 고객에게 이미 제출한 견적 금액은 기록으로 남아야 하므로 당시 결과를 그대로 보존한다(FN-09 R9-1).

### 5.4 데이터 관계도

```
MasterData (1)
    │ 참조 (계산 시점)
    ↓
QuoteInput (1) ──calculate()──► QuoteResult (1)
    │                                │
    └──────────┬─────────────────────┘
               ↓ 저장
      QuoteHistoryItem (N, 최대 50)
               │
               └─ 불러오기 → QuoteInput 복원 → 현재 MasterData로 재계산
```

---

## 6. 파생 데이터 및 계산 의존 관계

계산 순서가 어긋나면 결과가 틀어지므로 의존 관계를 고정한다.

```
입력값 검증 (FN-06)
   ↓
① weight        ← L, W, T, density
② material      ← ① , steel[type], moldBaseFactor, hotRunner
③ machining     ← t*, rate.*, complexity, cavities, cavityFactor
④ mechanism     ← slideCount, lifterCount, slideUnit, lifterUnit
⑤ totalCost     ← ② + ③ + ④ + trialRun
⑥ marginProfit  ← ⑤ × marginRate
⑦ finalPrice    ← ⑤ + ⑥ + overhead
   ↓
화면 렌더링 (9개 필드 + 경고 영역)
```

**단방향 의존만 존재하며 순환 참조가 없다.** 따라서 입력이 바뀔 때마다 전체를 처음부터 다시 계산하면 되고, 부분 갱신 최적화는 불필요하다(연산량이 수십 회 수준).

---

## 7. 완료 체크리스트

- [x] 전체 메뉴 구조(사이트맵)가 정의되었는가 → 2장
- [x] 모든 화면이 목록화되었는가 → 3장 (SCR-01~06)
- [x] 화면 간 이동 경로가 설계되었는가 → 4장 + 이동 규칙 N-1~N-6
- [x] 데이터 구조가 정의되었는가 → 5장 (저장소 2종, 엔티티 3종)
- [x] 계산 의존 관계에 순환이 없는가 → 6장
