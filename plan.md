# 사출금형 견적 시뮬레이터 — 개발기획서

- 작성일: 2026-09-18
- 근거 문서: `Research.md`
- 산출물: 단일 파일 `index.html` (서버·빌드·API 없음, 브라우저에서 바로 실행)

---

## 1. 제품 정의

| 항목 | 내용 |
|---|---|
| 목적 | 사출금형 견적을 현장에서 즉시 시뮬레이션하고 PDF 견적서로 출력 |
| 사용자 | 금형업체 영업/견적 담당자 (1인~소수, 사내 PC) |
| 형태 | 완전 독립형 클라이언트 웹앱. 외부 전송 없음 |
| 비목표 | 로그인, 다중 사용자, 서버 DB, ERP 연동, 3D 형상 해석 |

### 성공 기준
1. `index.html`을 더블클릭하면 콘솔 에러 0건으로 동작한다.
2. 입력값 변경 시 재계산이 즉시 반영된다.
3. PDF 다운로드가 실제 파일로 저장되고 항목·금액이 화면과 일치한다.
4. 브라우저를 껐다 켜도 마스터 단가와 견적 이력이 유지된다.

---

## 2. Research.md 검토 결과 (구현 전 반드시 수정)

Research.md의 예제 코드는 **그대로 쓰면 실행되지 않는다.** 확인된 결함:

| # | 위치 | 문제 | 조치 |
|---|---|---|---|
| R1 | `<head>` CDN | `https://jsdelivr.net`, `https://cloudflare.com` — 라이브러리 경로가 아닌 도메인 루트 | 정식 CDN 경로로 교체 (아래 3장) |
| R2 | `innerText = ${...} kg` | 템플릿 리터럴 백틱(`` ` ``) 누락 → **SyntaxError로 전체 스크립트 중단** | 백틱으로 감싸기 |
| R3 | `tableRows` 내부 | 같은 백틱 누락 | 동일 |
| R4 | `<script>` 블록 | 닫는 `</script>`, `</body>`, `</html>` 누락 | 보강 |
| R5 | `cavities` 입력 | 파싱만 하고 계산에 미사용 (죽은 입력값) | 가공 시간 가중에 반영하거나 입력 제거 — 5장 결정 필요 |
| R6 | 복잡도 스케일러(1.0~1.5) | 설계 문서에는 있으나 코드에 없음 | 구현에 포함 |
| R7 | 임율 35,000 / 50,000 등 | JS에 하드코딩 + UI 라벨에 중복 기재 → 단가 변경 시 두 곳 수정 | 단일 마스터 객체로 분리 |
| R8 | `moldBaseStructureCost = L * W * 1.2` | 근거 불명확한 상수 | 마스터 데이터로 노출해 사용자가 조정 가능하게 |
| R9 | jsPDF 한글 | 기본 폰트가 한글 미지원 → 깨짐 | 5장 결정 필요 |

> R2는 치명적이다. 이 한 줄 때문에 원문 코드는 화면에 아무 값도 표시하지 못한다.

---

## 3. 기술 스택

| 분류 | 선택 | 비고 |
|---|---|---|
| UI | Tailwind CSS (CDN) | `https://cdn.tailwindcss.com` |
| 로직 | Vanilla JS (ES6+) | 프레임워크 없음 |
| 저장 | `localStorage` | 마스터 단가 + 견적 이력 |
| PDF | jsPDF + jspdf-autotable (CDN) | `cdn.jsdelivr.net/npm/jspdf@2.5.1/...`, `.../jspdf-autotable@3.8.2/...` |

빌드 도구·패키지 매니저·번들러 없음. 파일 1개가 전부다.

---

## 4. 계산 엔진 사양

```
중량(kg)     = L × W × T / 1,000,000 × 7.85
재료비       = 중량 × 강재단가 + 몰드베이스비 + 핫러너비
가공비       = Σ(공정시간 × 공정임율) × 복잡도계수
기구비       = 슬라이드수 × 슬라이드단가 + 경사핀수 × 경사핀단가
제조원가     = 재료비 + 가공비 + 기구비 + 시사출비
최종공급가   = 제조원가 × (1 + 마진율/100) + 일반관리비
```

### 마스터 데이터 (단일 소스, localStorage 저장)
```js
const DEFAULT_MASTER = {
  steel:   { P20: 3500, NAK80: 5500, H13: 8000 },   // 원/kg
  rate:    { design: 35000, cnc: 50000, edm: 40000, assembly: 30000 }, // 원/hr
  moldBaseFactor: 1.2,      // L×W×factor
  slideUnit: 1200000,
  lifterUnit: 600000,
  trialRun: 500000,
  overhead: 0,              // 일반관리비
  complexity: { simple: 1.0, normal: 1.2, precision: 1.5 }
};
```
UI 라벨의 임율 표기는 이 객체에서 렌더링한다(하드코딩 금지, R7).

### 입력 검증 (생략 불가)
- 치수·시간·수량: 음수 및 NaN 거부, 0 이상으로 클램프
- 마진율: 0~100 범위 밖 경고 표시
- 최종가가 음수가 되는 조합은 계산 결과에 경고 배지 표시

---

## 5. 착수 전 확인 필요 (가정 명시)

아래는 임의로 정하지 않고 확인받아야 할 항목이다. 회신 전까지는 **괄호 안 기본값**으로 진행한다.

1. **PDF 한글 표기** — (기본: 원문대로 영문 레이아웃 + 금액만 숫자)
   - 대안 A: 영문 유지 → 코드 0줄 추가, 고객 제출용으로 어색
   - 대안 B: 나눔고딕 base64 임베드 → 완전 한글, 파일 크기 +2~3MB
   - 대안 C: `window.print()` + CSS `@media print` → **의존성 0, 한글 완벽**, 대신 "PDF 저장" 수동 선택
   - 권장: **C**. jsPDF 두 개를 걷어내고도 목적을 달성한다.
2. **캐비티 수 처리** (R5) — (기본: 가공시간 가중 `1 + (cavity-1) × 0.15`에 반영)
3. **일반관리비** — 원문 공식에는 있으나 코드에 없음 (기본: 0원, 마스터에서 조정)
4. **고객사/프로젝트명 입력란** — 견적서에 필요 (기본: 포함)

---

## 6. 단계별 구현 계획

### Phase 1 — 동작하는 베이스라인 (필수)
- Research.md 코드를 옮기되 R1~R4 결함 수정
- 마스터 데이터 객체 분리(R7), 임율 라벨 자동 렌더링
- 입력 검증 추가
- **검증**: 브라우저 콘솔 에러 0건 / 기본값(400×400×350, NAK80) 입력 시 7개 결과 필드 모두 숫자 표시

### Phase 2 — 견적 정확도
- 복잡도 드롭다운 + 가공비 가중(R6)
- 캐비티 반영(5장 2번 결정 적용)
- 일반관리비 필드
- **검증**: 복잡도 일반→초정밀 변경 시 가공비만 1.5배, 재료비 불변

### Phase 3 — 출력
- 5장 1번 결정에 따라 PDF 또는 인쇄 레이아웃 구현
- 고객사/프로젝트/견적일/유효기간 헤더
- **검증**: 출력물 금액 7개 항목이 화면 값과 완전 일치

### Phase 4 — 데이터 보존
- 마스터 단가 편집 패널 + `localStorage` 저장/초기화
- 견적 이력 저장·목록·불러오기·삭제 (최근 50건)
- **검증**: 견적 저장 → 브라우저 재시작 → 동일 값 복원

### Phase 5 — 마감
- 반응형 확인(1280px / 노트북 1366px)
- 초기값 세트 1건으로 손계산 대조표 작성
- **검증**: 손계산 결과와 앱 결과 오차 0원

Phase 1~3이 실사용 최소 단위다. Phase 4~5는 이후 진행해도 무방하다.

---

## 7. 검증 방법

별도 테스트 프레임워크를 도입하지 않는다. 대신:
- `index.html` 하단에 개발용 콘솔 자가검증 함수 1개(`__selfTest()`)를 두고, 알려진 입력 3세트의 기대 금액과 비교한다.
- 회귀 확인은 이 함수 실행 한 번으로 끝낸다.

| 케이스 | 입력 | 확인 항목 |
|---|---|---|
| T1 | 400×400×350, NAK80, 기본시간 | 중량 439.6kg, 최종가 손계산 일치 |
| T2 | 치수 0 입력 | 0원 출력, 에러 없음 |
| T3 | 마진율 -10 입력 | 경고 표시, 계산 중단 없음 |

---

## 8. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 견적 계수(임율·기구 단가)가 업체 실제 원가와 불일치 | 견적 오차 | 마스터 데이터 편집 UI로 사용자가 직접 교정 |
| CDN 접속 불가한 폐쇄망 | 앱 미동작 | Tailwind 결과 CSS와 라이브러리를 파일에 인라인화(오프라인 모드) |
| localStorage 초기화로 마스터 단가 소실 | 재입력 부담 | JSON 내보내기/가져오기 버튼(Phase 4에 포함) |
| 견적서가 법적 효력 문서로 오인 | 분쟁 | 출력물 하단 "참고용 추정 견적, 부가세 별도" 고정 문구 |

---

## 9. 다음 액션
1. 5장 4개 항목 확인 (특히 PDF 방식)
2. Phase 1 구현 → `index.html` 생성
3. T1 손계산 대조표 작성 후 결과 보고

---

## 10. 실행·평가 기록

### Phase 1 — 동작하는 베이스라인 (완료, 2026-09-19)

**변경 파일**: `index.html` (신규 생성)

**적용 내용**:
- R1: Tailwind CDN을 `https://cdn.tailwindcss.com`(정식 경로)로 교체, jsPDF CDN은 제외(D-01 확정값이 `window.print()`이므로 Phase 1에 불필요 — Phase 3에서 결정에 따라 구현)
- R2/R3: `innerText` 대입부 전체를 백틱 템플릿 리터럴로 수정 (원본은 파싱 자체가 실패하는 SyntaxError 상태였음)
- R4: `</script></body></html>` 보강
- R7: `DEFAULT_MASTER` 객체로 강재 단가·공정 임율·몰드베이스 계수·기구 단가·시사출비를 분리. 강재 select 옵션과 공정 임율 라벨(`설계/CAD (임율: 35,000원)` 등)을 `renderMasterLabels()`가 마스터 객체에서 렌더링 — 하드코딩 텍스트 제거
- 입력 검증: `toNonNegativeNumber()`로 모든 수치 입력(치수·시간·수량·마진율)을 NaN/음수 시 0으로 클램프
- R5(캐비티) 입력은 유지하되 계산에는 아직 미반영 — Phase 2에서 D-02(`1 + (cavity-1) × 0.15`) 적용 예정 (의도된 임시 상태)
- Phase 2 대상(복잡도 가중, 캐비티 반영, 일반관리비 UI)과 Phase 3 대상(PDF/인쇄 출력)은 이번 범위에서 제외

**검증 결과** (Claude Code 내장 브라우저로 실행, `file:///E:/AI/Mold-Cost-Estimator/index.html`):
- 콘솔 에러 0건 확인 (Tailwind CDN 프로덕션 경고 1건만 존재, 에러 아님)
- 기본값(400×400×350, NAK80, 기본 공정시간) 입력 시 7개 결과 필드 모두 숫자로 표시됨:
  - 중량 439.6kg / 재료비 2,609,800원 / 가공비 4,900,000원 / 기구비 2,400,000원 / 제조원가 10,409,800원 / 마진금액 2,602,450원 / **최종가 13,012,250원**
  - T1 케이스 기대값(중량 439.6kg)과 일치
- 방어 테스트: 가로에 `-100` 입력, 마진율에 `abc` 입력 → 콘솔 에러 없이 각각 0으로 클램프되어 재계산됨 (중량 0.0kg, 마진금액 0원, 나머지 필드는 정상 수치 유지)

**Phase 1 완료 기준 충족**: `plan.md` §6 Phase 1 검증 조건(콘솔 에러 0건, 7개 필드 숫자 표시) 통과.

**다음 단계**: Phase 2(복잡도 가중치 R6, 캐비티 반영 D-02, 일반관리비 필드) 착수 필요.

### Phase 2 — 견적 정확도 (완료, 2026-09-19)

**변경 파일**: `index.html`

**적용 내용**:
- R6: `DEFAULT_MASTER.complexity = { simple: 1.0, normal: 1.2, precision: 1.5 }` 추가, "형상 복잡도" 드롭다운(일반/복잡/초정밀)을 신설해 `renderMasterLabels()`가 마스터 데이터에서 옵션을 렌더링
- D-02: 캐비티 입력을 계산에 반영. `cavityFactor = 1 + (cavity-1) × 0.15`(`DEFAULT_MASTER.cavityStep`)를 가공비에 곱함. 캐비티는 1 미만 입력 시 1로 클램프(0캐비티는 물리적으로 불가능)
- D-03: "일반관리비 (원)" 입력 필드 추가. 기존에 상수(`DEFAULT_MASTER.overhead`)로 고정 가산하던 것을 사용자 입력값으로 대체(초기값은 마스터의 0을 그대로 사용)
- 가공비 계산식: `totalMachining = Σ(공정시간×임율) × 복잡도계수 × 캐비티계수` — 재료비 계산식은 변경하지 않음(복잡도·캐비티와 무관함을 유지)

**검증 결과** (Claude Code 내장 브라우저, `file:///E:/AI/Mold-Cost-Estimator/index.html`):
- 콘솔 에러 0건
- **plan.md §6 Phase 2 검증 조건 충족**: 복잡도를 일반(1.0)→초정밀(1.5)로 변경 시 가공비만 정확히 1.5배(5,635,000원 → 8,452,500원, ratio=1.5), 재료비는 2,609,800원으로 불변
- 캐비티 음수(-5) 입력 → 1로 클램프되어 캐비티계수 1.0 적용, 에러 없음
- 일반관리비 문자("xyz") 입력 → 0으로 클램프, 최종가에 정상 반영, 에러 없음

**다음 단계**: Phase 3(D-01 확정값에 따른 `window.print()` + `@media print` 출력, 고객사/프로젝트명 헤더) 착수 필요.

### Phase 3 — 출력 (완료, 2026-09-19)

**변경 파일**: `index.html`

**적용 내용**:
- D-01 확정값 그대로 구현: jsPDF 미도입, `window.print()` 버튼 + `@media print` CSS로 인쇄 시 `#printArea`만 보이도록 처리(다른 요소는 `visibility: hidden`)
- D-04: "0. 견적서 기본 정보" 섹션 신설 — 고객사명, 프로젝트명, 견적일(기본값: 오늘 날짜), 유효기간(일, 기본 30일) 입력
- 유효기간은 견적일 + 유효기간(일)로 만료일을 계산해 "견적일로부터 N일 (YYYY-MM-DD까지)" 형식으로 표시
- REQ-19: 출력물 상단에 "본 견적서는 참고용 추정 견적이며, 부가세는 별도입니다." 고정 문구 삽입(협상 대상 아님)
- 인쇄 영역(`#printArea`)의 7개 금액 필드는 화면 결과 패널과 **동일한 `computedData` 객체**를 `calculateQuote()` 한 함수 안에서 동시에 바인딩 — 값이 어긋날 수 있는 별도 계산 경로를 두지 않음

**검증 결과** (Claude Code 내장 브라우저, `file:///E:/AI/Mold-Cost-Estimator/index.html`):
- 콘솔 에러 0건
- **plan.md §6 Phase 3 검증 조건 충족**: 고객사명("대한전자(주)")·프로젝트명("도어트림 금형") 입력 후 화면 결과 7개 필드와 인쇄 영역 7개 필드를 전수 대조 — 전 항목 완전 일치(중량 439.6kg, 재료비 2,609,800원, 가공비 5,635,000원, 기구비 2,400,000원, 제조원가 11,144,800원, 마진 2,786,200원, 최종가 13,931,000원)
- 견적일 기본값이 오늘 날짜(로컬 PC 기준)로 자동 설정됨, 유효기간 30일 적용 시 만료일 자동 계산 확인(2026-09-19 → 2026-10-19)
- 실제 인쇄 대화상자 렌더링(OS 프린터 드라이버 호출)은 헤드리스 브라우저 환경 특성상 이 세션에서 직접 확인 불가 — `@media print` 규칙과 인쇄 전용 DOM 구조까지는 검증 완료. **사용자가 실제 PC에서 "확정 견적서 인쇄 / PDF로 저장" 버튼을 눌러 인쇄 미리보기 화면을 육안으로 한 번 확인할 것을 권장.**

**다음 단계**: Phase 4(마스터 데이터 편집 패널 + localStorage 저장, 견적 이력 저장/조회/삭제) 착수 필요.

### Phase 4 — 데이터 보존 (완료, 2026-09-19)

**변경 파일**: `index.html`, `E:\AI\.claude\launch.json`(검증용 정적 서버 항목 `mold-cost-estimator` 추가 — 기존 다른 프로젝트 항목들과 나란히, 삭제하지 않음)

**적용 내용**:
- REQ-08/REQ-18: `DEFAULT_MASTER`(불변 기본값)와 별도로 `let MASTER`(가변 상태)를 도입. 페이지 로드 시 `localStorage['moldEstimator_master_v1']`가 있으면 그 값을, 없으면 `DEFAULT_MASTER`를 복사해 사용. 계산 엔진(`calculateQuote`)과 옵션 렌더링(`renderMasterLabels`)의 `DEFAULT_MASTER` 참조를 전부 `MASTER`로 교체
- "5. 마스터 데이터 관리" 패널(`<details>` 접이식) 신설 — 강재 단가 3종, 공정 임율 4종, 몰드베이스 계수, 슬라이드/경사핀 단가, 시사출비, 캐비티 가중 스텝, 복잡도 가중치 3종을 편집 가능. **저장**(localStorage 반영 + 즉시 재계산) / **기본값으로 초기화**(localStorage 삭제 + `DEFAULT_MASTER` 복원) / **JSON 내보내기**(Blob 다운로드) / **JSON 가져오기**(FileReader로 읽어 형태 검증 후 적용, 실패 시 alert) 버튼 구현. 저장/초기화 시 모든 편집값에 `toNonNegativeNumber` 방어 적용
- REQ-10: "4. 견적 이력" 패널 신설. "현재 견적 저장" 클릭 시 폼 전체 상태(`FORM_FIELD_IDS` 19개 필드)와 계산 결과를 레코드로 만들어 `localStorage['moldEstimator_history_v1']` 배열 맨 앞에 추가, **최근 50건으로 자동 상한**(`slice(0, 50)`). 각 레코드는 저장 시각·고객사·프로젝트명·최종가로 목록에 표시되며 **불러오기**(폼 전체 복원 후 재계산)와 **삭제** 버튼 제공
- 검증 방법: 로컬 파일(`file://`)은 브라우저가 `data:` URL로 정적 렌더링해 `localStorage`가 비활성화되므로, 이 세션에서만 임시로 `python -m http.server`를 붙여(`E:\AI\.claude\launch.json`에 `mold-cost-estimator` 항목 추가, 포트 8888) `http://localhost` 오리진에서 검증. 실제 사용자는 `index.html`을 그대로 더블클릭해 열면 되며 이는 REQ-16과 무관(파일 프로토콜에서도 `localStorage`는 정상 동작 — 이번 세션의 브라우저 프리뷰 도구 자체의 `data:` 스냅샷 제약이었을 뿐)

**검증 결과** (`http://localhost:8888/index.html`, Claude Code 내장 브라우저):
- 콘솔 에러 0건
- 마스터 데이터: CNC 임율을 50,000→60,000원으로 수정 후 저장 → `MASTER.rate.cnc`, 화면 라벨, `localStorage` 저장값 모두 60,000으로 일치 확인. **페이지 새로고침(브라우저 재시작 시뮬레이션) 후에도 60,000 유지** — 마스터 편집 폼, 라벨, 계산 결과(가공비) 전부 복원됨을 확인 → **plan.md §6 Phase 4 검증 조건("저장 → 재시작 → 동일 값 복원") 충족**
- 견적 이력: 고객사/프로젝트명 입력 후 저장 → `localStorage`에 레코드 1건 기록. **새로고침 후에도 이력 목록에 그대로 표시**됨을 확인. "불러오기" 클릭 시 폼 전체(고객사명 포함) 정확히 복원, "삭제" 클릭 시 해당 레코드만 제거되고 목록이 빈 상태로 갱신됨을 확인
- 마스터 데이터 "기본값으로 초기화" → CNC 임율이 50,000원으로 복귀하고 `localStorage` 키가 삭제됨을 확인
- JSON 가져오기: 수정된 마스터 객체를 담은 파일을 `importMasterData()`에 주입 → 값 반영 및 `localStorage` 갱신 확인(가져오기 실패 시 alert 분기는 코드 검토로 확인, 별도 예외 케이스 실행 테스트는 생략)

**남은 리스크**: 견적 이력이 50건을 초과하는 실사용 시나리오(오래된 레코드 자동 삭제)는 로직상 `slice(0, 50)`으로 보장되나 50건 실제 적재 테스트는 수행하지 않음.

**다음 단계**: Phase 5(반응형 확인, 손계산 대조표, `__selfTest()` 자가검증 함수) — 실사용 최소 단위(Phase 1~3)는 이미 충족되어 있어 필요 시에만 진행.

### Phase 5 — 마감 (완료, 2026-09-19)

**변경 파일**: `index.html`

**적용 내용**:
- `__selfTest()` 함수 추가(§7 요구사항). 브라우저 콘솔에서 직접 실행하는 개발용 회귀 검증 함수로, UI 버튼은 두지 않음(스펙에 "개발용 콘솔" 함수로 명시됨)
- 사용자가 Phase 4에서 마스터 데이터를 편집해 두었더라도 검증 결과가 흔들리지 않도록, 테스트 실행 중에는 `MASTER`를 `DEFAULT_MASTER`의 깊은 복사본으로 임시 교체하고 종료 시 원래 `MASTER`·폼 상태를 전부 복원
- T1(기본값)·T2(치수 0)·T3(마진율 음수) 3케이스를 §7 표 그대로 구현. **T3는 SRS가 원래 요구한 "경고 표시" 대신 Phase 1에서 이미 채택한 "0으로 클램프" 방어 방식을 기준으로 기대값을 정의** — 별도 경고 UI는 이번 범위에 없음(Phase 1~4 어디에도 구현되지 않았고, 지금 새로 추가하는 것은 범위 확장이라 보류)
- 반응형은 이미 Phase 1~4에서 Tailwind 그리드(`grid-cols-1 lg:grid-cols-2`)로 처리돼 있어 별도 CSS 변경 없이 검증만 수행

**검증 결과** (Claude Code 내장 브라우저, `file:///E:/AI/Mold-Cost-Estimator/index.html`):
- 콘솔 에러 0건
- `__selfTest()` 실행 결과 T1·T2·T3 **3케이스 전원 PASS** (콘솔 `console.table` 출력으로 확인)
- 1280px, 1366px 각각에서 `document.documentElement.scrollWidth`가 뷰포트 너비보다 작음을 확인 — **가로 스크롤 없음**(REQ-17 충족): 1280px→1265px, 1366px→1351px

**T1 손계산 대조표** (기본값 400×400×350mm, NAK80, 캐비티 2, 복잡도 일반, 마진 25%):

| 항목 | 계산식 | 손계산 결과 | 앱 출력값 | 오차 |
|---|---|---|---|---|
| 중량 | 400×400×350/1,000,000×7.85 | 439.6 kg | 439.6 kg | 0 |
| 재료비 | 439.6×5,500 + 400×400×1.2 + 0 | 2,417,800 + 192,000 = 2,609,800원 | 2,609,800원 | 0원 |
| 캐비티 계수 | 1+(2-1)×0.15 | 1.15 | (내부값, 미표시) | - |
| 가공비(가중 전) | 20×35,000+45×50,000+30×40,000+25×30,000 | 700,000+2,250,000+1,200,000+750,000=4,900,000원 | - | - |
| 가공비(최종) | 4,900,000×1.0(복잡도)×1.15(캐비티) | 5,635,000원 | 5,635,000원 | 0원 |
| 기구비 | 1×1,200,000 + 2×600,000 | 2,400,000원 | 2,400,000원 | 0원 |
| 제조원가 | 2,609,800+5,635,000+2,400,000+500,000 | 11,144,800원 | 11,144,800원 | 0원 |
| 마진금액 | 11,144,800×0.25 | 2,786,200원 | 2,786,200원 | 0원 |
| **최종 공급가** | 11,144,800+2,786,200+0 | **13,931,000원** | **13,931,000원** | **0원** |

→ **plan.md §6 Phase 5 검증 조건("손계산 결과와 앱 결과 오차 0원") 충족.**

**Phase 1~5 전체 완료.** MVP 및 2차 범위(SRS §3.1~3.2) 기능이 모두 `index.html` 한 파일에 구현되었고, 각 Phase의 검증 조건을 모두 통과했다. 이후 작업은 실사용 중 발견되는 버그 수정이나 SRS §3.3 제외 범위 재검토 시에만 이어서 진행하면 된다.
